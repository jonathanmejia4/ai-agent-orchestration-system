---
issue_id: "M-01"
lane: "M"
severity: 6
severity_level: "MEDIUM"
type_tags: ["schema", "config"]
status: "OPEN"
---

# [LANE M] Issue M-01: Schema version field missing in config

## Problem Description

The schema config lacks a version field for tracking schema changes.

## Evidence

- File: `examples/minimal_demo/config/schema.yaml`
- Missing: `schema_version` field

## affected_paths
  - examples/minimal_demo/config/schema.yaml

## Fix Requirements

Add `schema_version: "1.0.0"` to the top of the schema config.

## Verification Commands

```bash
grep -q "schema_version:" examples/minimal_demo/config/schema.yaml && echo "PASS" || echo "FAIL"
```

- **Status:** OPEN
