---
issue_id: "G-01"
lane: "G"
severity: 4
severity_level: "MEDIUM"
type_tags: ["G-Issue", "Ghost-Reference", "Doc-Drift"]
status: "OPEN"
affected_paths:
  - "README.md"
---

# [LANE G] Issue G-01: README references 23 lanes but 26 exist (A-Z)

- Type Tags: G-Issue, Ghost-Reference, Doc-Drift
- Severity: 4/10 (MEDIUM)
- Status: OPEN
- Date Discovered: 2026-04-23

---

## Problem Description

- **What is wrong:** `README.md` claims the framework has **23 specialized agent lanes** and **23 matching fixer agents**, but the actual count is **26** of each (A-Z inclusive).
- **Expected:** README should reflect the real lane count (26 hunters + 26 fixers) that new users will see when cloning the repo.
- **Actual:** Seven occurrences of the number `23` in contexts like "23 specialized agent lanes", "23 matching fixer agents", "23 lane hunters + orchestrator", etc.
- **Scope:** Public-facing README — this is the first document strangers read. A drift between stated and actual lane count undermines the credibility of every other claim in the README.

## Evidence

```bash
$ ls .claude/agents/issue-hunters/IH-Lane-*.md | wc -l
      26

$ ls .claude/agents/issue-fixers/IF-Lane-*.md | wc -l
      26

$ grep -n "\b23\b" README.md
35:- **23 specialized agent lanes** (A-Z) that hunt for issues in parallel
36:- **23 matching fixer agents** that resolve issues automatically
44:- **Cost:** ~265,000 tokens for 23 agents = context window blown
56:| `/find-all` | Hunt for issues across all 23 lanes in parallel |
77:All 23 lanes are customizable. See [CUSTOMIZATION_GUIDE.md](CUSTOMIZATION_GUIDE.md).
82:Orchestrator: Spawns 23 hunter agents in parallel
84:Orchestrator: Polls ls signals/*.done until all 23 complete
110:│   │   ├── issue-hunters/      # 23 lane hunters + orchestrator
111:│   │   └── issue-fixers/       # 23 lane fixers + orchestrator
```

- **Source:** `README.md:35`
  > "- **23 specialized agent lanes** (A-Z) that hunt for issues in parallel"
- **Source:** `README.md:36`
  > "- **23 matching fixer agents** that resolve issues automatically"

## Fix Requirements (DO NOT IMPLEMENT)

- [ ] Replace every occurrence of `23` that refers to lane count with `26` in `README.md`
- [ ] Keep the `~265,000 tokens` and `~3,000 tokens` cost figures intact — those are unrelated to the `23` lane-count reference on the same line; rewrite the 23-agent prose without changing the economics
- [ ] Leave any `23` that is not describing lanes alone (there are none today, but guard against false positives)

## Verification Commands

```bash
# Check 1: No stale "23 ... lanes" / "23 ... agents" references remain
! grep -nE "\b23\b.*(lane|agent|hunter|fixer)" README.md && echo "PASS" || echo "FAIL"

# Check 2: README explicitly uses the correct count somewhere
grep -qE "\b26\b.*(lane|agent|hunter|fixer)" README.md && echo "PASS" || echo "FAIL"

# Check 3: Lane count on disk matches what README claims
test "$(ls .claude/agents/issue-hunters/IH-Lane-*.md | wc -l | tr -d ' ')" = "26" && echo "PASS" || echo "FAIL"
```

## Dedup Verification

- **Search terms:** "readme references 23 lanes but 26 exist (a-z)", "23 lanes", "23 agents"
- **Files checked:** issues/G/, ISSUE_CATALOG.md
- **Result:** No duplicates found
